#include "main.h"
/**
 * main - tests the postitive_or_negative() function at zero
 * Return: 0
 */
int main(void)
{
        int i;
        i = 0;
        positive_or_negative(i);
        return (0);
}
